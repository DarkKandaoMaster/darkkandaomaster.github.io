{
  "name": "kandao-personal-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "翁路凯 / DarkKandaoMaster 的个人网站",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check scripts/serve.mjs && node --check scripts/build.mjs && node --check assets/site.js",
    "test": "playwright test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0"
  }
}
